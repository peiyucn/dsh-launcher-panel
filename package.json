{
  "name": "dsh-launcher-panel",
  "displayName": "DSH Launcher Panel",
  "description": "Start DeepSeek Harness (dsh) and open its web UI in VS Code's built-in browser.",
  "version": "0.2.7",
  "icon": "resources/icon.png",
  "publisher": "peiyucn",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/peiyucn/dsh-launcher-panel.git"
  },
  "homepage": "https://pyai.site/",
  "bugs": {
    "url": "https://github.com/peiyucn/dsh-launcher-panel/issues"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "keywords": [
    "deepseek",
    "deepseek-harness",
    "dsh",
    "launcher",
    "panel",
    "ai",
    "coding-agent",
    "web-ui"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "iconFonts": [
      {
        "id": "dsh-icon-font",
        "src": [
          {
            "path": "resources/dsh-icon.woff",
            "format": "woff"
          }
        ]
      }
    ],
    "icons": {
      "dsh-whale": {
        "description": "DSH whale (spouting)",
        "default": {
          "fontPath": "resources/dsh-icon.woff",
          "fontCharacter": "\\e900"
        }
      },
      "dsh-whale-splash-small": {
        "description": "DSH whale, small splash (animation frame)",
        "default": {
          "fontPath": "resources/dsh-icon.woff",
          "fontCharacter": "\\e901"
        }
      },
      "dsh-whale-splash-large": {
        "description": "DSH whale, large splash (animation frame)",
        "default": {
          "fontPath": "resources/dsh-icon.woff",
          "fontCharacter": "\\e902"
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "dsh",
          "title": "DSH WebUI",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "dsh": [
        {
          "type": "webview",
          "id": "dsh.panel",
          "name": "Dashboard",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "colors": {
      "dsh.statusBar.runningBackground": {
        "dark": "#4176E6",
        "light": "#4176E6",
        "highContrast": "#4176E6"
      },
      "dsh.statusBar.runningForeground": {
        "dark": "#FFFFFF",
        "light": "#FFFFFF",
        "highContrast": "#FFFFFF"
      }
    },
    "commands": [
      {
        "command": "dsh.start",
        "title": "Start",
        "category": "DSH Launcher Panel"
      },
      {
        "command": "dsh.stop",
        "title": "Stop",
        "category": "DSH Launcher Panel"
      }
    ],
    "configuration": {
      "title": "DSH Launcher Panel",
      "properties": {
        "dsh.runMode": {
          "order": 1,
          "type": "string",
          "enum": [
            "pnpm",
            "source"
          ],
          "default": "pnpm",
          "description": "How to run DeepSeek Harness: 'pnpm' installs dsh into a launcher-managed location and runs 'pnpm exec dsh web'; 'source' runs a local checkout via tsx. Normally set with the mode pill in the panel."
        },
        "dsh.npmChannel": {
          "order": 2,
          "type": "string",
          "enum": [
            "latest",
            "next",
            "alpha"
          ],
          "default": "latest",
          "description": "Which npm dist-tag pkg mode installs @deepseek-ai/dsh from on first install, and which version the Update button targets: 'latest' (stable), 'next' (release candidates), or 'alpha' (alphas). An installed dsh always starts as-is — the channel never downgrades or auto-upgrades it. Source mode ignores this and always tracks the newest official dsh-v<version> git tag."
        },
        "dsh.pkgPath": {
          "order": 3,
          "type": "string",
          "default": "",
          "description": "Optional: custom directory where pkg mode installs dsh. When empty, the extension uses a managed default location."
        },
        "dsh.srcPath": {
          "order": 4,
          "type": "string",
          "default": "",
          "description": "Optional: path to an existing deepseek-harness clone for source mode. When empty, the extension clones the repo automatically."
        },
        "dsh.nodePath": {
          "order": 5,
          "type": "string",
          "default": "",
          "description": "Path to node.exe. Leave empty to use the node on PATH."
        },
        "dsh.port": {
          "order": 6,
          "type": "number",
          "default": 3080,
          "description": "Port the web UI listens on."
        },
        "dsh.autoOpenBrowser": {
          "order": 7,
          "type": "boolean",
          "default": true,
          "description": "Automatically open the browser after dsh starts. Turn off to keep your current tab (the official page shows a reconnect prompt); the Start button's 'New Tab' click still opens one per dsh.browser."
        },
        "dsh.browser": {
          "order": 8,
          "type": "string",
          "enum": [
            "built-in",
            "external"
          ],
          "default": "built-in",
          "description": "Which browser to open the UI in. 'built-in' uses VS Code's Simple Browser (falls back to external if disabled); 'external' opens the system browser."
        },
        "dsh.hideConsole": {
          "order": 9,
          "type": "boolean",
          "default": true,
          "description": "Run the DSH server in a hidden console so neither it nor the tool subprocesses (bash/pwsh) flash a cmd window. Turn off to show it for debugging."
        },
        "dsh.clearServerLogOnStart": {
          "order": 10,
          "type": "boolean",
          "default": true,
          "description": "Clear the server log file at the start of each launch so it only contains the current run."
        },
        "dsh.sourceDebug": {
          "order": 11,
          "type": "boolean",
          "default": false,
          "description": "In source mode, enable NODE_DEBUG=module to print module-loading progress (very verbose; the console shows a periodic count, full detail is in the server log)."
        }
      }
    }
  },
  "scripts": {
    "build:icon-font": "node scripts/build-icon-font.mjs",
    "typecheck": "tsc -p ./ --noEmit",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "package": "vsce package",
    "test": "node --import tsx --test",
    "verify": "npm run typecheck && npm test && npm run compile && npm run package"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.0.0",
    "svg2ttf": "^6.1.0",
    "svgicons2svgfont": "^16.0.0",
    "tsx": "^4.23.12",
    "ttf2woff": "^3.0.0",
    "typescript": "^5.5.0"
  }
}
